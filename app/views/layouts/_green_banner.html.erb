<% if customer_signed_in? %>
    <div class="container top-green-status <%= params[:kind] == :adult ? "violet-bck" : "green-bck" %> black position-relative">
      <div class="position-absolute" id="user-status-switch">
        <i class="fa fa-copy"></i> <a href="index.html#" class="no-decoration">(voir tout)</a>
      </div>
      <div class="text-center" id="promo-details">
        <span class="hidden-xs">Vous n'avez pas de crédits disponibles</span>
      </div>
    </div>
    <div class="container pad0">
      <div class="white-bck panel-enter-code block100">
        <div class="col-md-6 col-sm-6 col-xs-12 padr10 padtb20 mart15">
          <span><a class="<%= params[:kind] == :adult ? "violet" : "blue" %> underline" href="index.html">Plush à la Carte</a></span>
          <span class="black mart5">crédits disponibles: 0</span>
          <span class="black mart5"> Votre abonnement: pas <strong>d’abonnement actif</strong> <a href="index.html" class="<%= params[:kind] == :adult ? "violet" : "blue" %> underline">(souscrire)</a></span>


          <span class="mart25"><a class="<%= params[:kind] == :adult ? "violet" : "green" %> underline" href="index.html">Plush Unlimited</a></span>
          <span class="black mart5">Votre abonnement: pas d’abonnement actif <a href="index.html" class="<%= params[:kind] == :adult ? "violet" : "green" %> underline">(souscrire)</a></span>

        </div>
        <div class="col-md-6 col-sm-6 col-xs-12 padtb20 mart15">
          <span class="bold">Vous avez un code promo ?</span> <span>
                        <input type="text" name="code" placeholder="Code" class="black pad5 pull-left">
                        <input type="submit" name="submit-code" value="Valider" class="pad5 <%= params[:kind] == :adult ? "violet-bck" : "blue-bck" %> white marl5 pull-left">
                    </span>
        </div>
      </div>
    </div>
<% else %>
    <div class="container top-green-status <%= params[:kind] == :adult ? "violet-bck" : "green-bck" %> black position-relative">
      <div class="position-absolute" id="user-status-switch">
        <i class="fa fa-copy"></i> <a href="index.html#" class="no-decoration">(voir tout)</a>
      </div>
      <div class="text-center" id="promo-details">
        <span class="hidden-xs"><%= t('.top_part_1_1') %> <%= t('.visitor_no_credits') %>  <%= t('.top_part_2') %></span>
      </div>
    </div>
    <div class="container pad0">
      <div class="white-bck panel-enter-code block100">
        <div class="col-md-6 col-sm-6 col-xs-12 padr10 padtb20 mart15">
          <span><a class="<%= params[:kind] == :adult ? "violet" : "blue" %> underline" href="<%= products_short_path(:package => Moovies.packages.invert[(params[:kind] == :adult ? 5 : 2)]) %>">Plush à la Carte</a></span>
          <span class="black mart5"><%= t('.top_part_2') %>: 0</span>
          <span class="black mart5"> <%= t('.subscription') %>: <strong><%= t('.subscription_info') %></strong> <a href="<%= info_path(:page_name => t('routes.infos.params.alacarte'))%>" class="<%= params[:kind] == :adult ? "violet" : "blue" %> underline">(<%= t('.subscribe') %>)</a></span>
          <span class="mart25"><a class="<%= params[:kind] == :adult ? "violet" : "green" %> underline" href="<%= products_short_path(:package => Moovies.packages.invert[(params[:kind] == :adult ? 4 : 1)]) %>">Plush Unlimited</a></span>
          <span class="black mart5"><%= t('.subscription') %>: <strong><%= t('.subscription_info') %></strong> <a href="<%= info_path(:page_name => t('routes.infos.params.unlimited'))%>" class="<%= params[:kind] == :adult ? "violet" : "green" %> underline">(<%= t('.subscribe') %>)</a></span>
        </div>
        <div class="col-md-6 col-sm-6 col-xs-12 padtb20 mart15">
          <span class="bold">Vous avez un code promo ?</span> <span>
                        <input type="text" name="code" placeholder="Code" class="black pad5 pull-left">
                        <input type="submit" name="submit-code" value="Valider" class="pad5 <%= params[:kind] == :adult ? "violet-bck" : "blue-bck" %> white marl5 pull-left">
                    </span>
        </div>
      </div>
    </div>
<% end %>


