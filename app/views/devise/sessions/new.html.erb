<div class="step2">
<%= link_to root_path do %>
  <%= image_tag "step2/logo.png" %>
<% end %>
<%= button_to t('.social.network.fbconnect'), '/auth/facebook/', :class => 'face-button' %>
</br>
<div class="separator-part">
  <div class="separator-line"></div>
    <label class="separator-label">OU</label>
  <div class="separator-line"></div>
</div>
<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
<% if alert && !alert.empty?%>
  <%= alert %> 
<% end %>
<%= f.text_field :email, :class => 'text-field', :tye => 'text', :placeholder => t('.email_label') %>
<%= f.password_field :password, :class => 'text-field', :maxlength => '40', :placeholder => t('.pass_label') %>
</br>
<% if devise_mapping.rememberable? -%>
    <div class="customer_remember_me hvr-grow">
      <%= f.check_box :remember_me, :checked => true %>
      <label for="customer_remember_me">
        <span></span>
      </label>
    </div>
    <label class="customer_remember_me_label"><%= t('.remember_me') %></label>
<% end -%>
<%= f.submit t('.connect'), :id => 'submit_button', :class => "button-connenct" %>
<% end %>
</div>
<div class="links">
  <%= render "devise/shared/links" %>
</div>
